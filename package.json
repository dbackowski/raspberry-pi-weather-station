{
  "name": "raspberry-pi-weather-station",
  "version": "1.0.0",
  "scripts": {
    "front": "cd front && yarn start",
    "api": "cd api && nodemon server.js",
    "start": "concurrently --kill-others-on-fail \"yarn api\" \"yarn front\""
  },
  "dependencies": {
    "express": "^4.16.3",
    "global": "^4.3.2",
    "nodemon": "^1.17.5"
  },
  "devDependencies": {
    "concurrently": "^3.6.0"
  }
}
